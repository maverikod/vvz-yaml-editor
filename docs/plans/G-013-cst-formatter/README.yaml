format: global_package_step_v2
plan_id: ai_editor_pip_package_for_mcp
step_id: G-013
step_name: cst_formatter
title: Built-in CST formatter backend for Python files
status: draft
source_plan: docs/plans/create_pip_package_for_mcp.yaml
source_documents:
- docs/tech_spec.md
- cst-code/README.md
global_intent: Own the built-in CST formatter for .py files. Adapts the existing cst-code
  subsystem into an AbstractFormatter implementation. The document in buffer is a
  CSTTree. The formatter owns parse/render/mutate/query semantics for Python source
  via LibCST. It does not open, save, or close files.
why_this_block_exists: Python files require structural editing at the AST/CST level,
  not line ranges. The cst-code subsystem already implements tree loading, stable
  node identity, mutation operations, sidecar caching, and XPath-like query. This
  block formalizes that code as a first-class AbstractFormatter so editor core, search,
  and public interfaces treat .py files uniformly with other formats.
owned_code_paths:
- ai_editor/formatters/cst/
- tests/formatters/cst/
owned_docs_paths:
- docs/cst_formatter.md
source_code_origin:
  path: cst-code/
  policy: adapt-not-rewrite
  take:
  - code_analysis/core/cst_tree/models.py
  - code_analysis/core/cst_tree/tree_builder.py
  - code_analysis/core/cst_tree/tree_modifier.py
  - code_analysis/core/cst_tree/tree_modifier_ops.py
  - code_analysis/core/cst_tree/tree_modifier_ops_find.py
  - code_analysis/core/cst_tree/tree_modifier_ops_insert.py
  - code_analysis/core/cst_tree/tree_modifier_ops_parse.py
  - code_analysis/core/cst_tree/tree_modifier_ops_replace.py
  - code_analysis/core/cst_tree/tree_modifier_validate.py
  - code_analysis/core/cst_tree/tree_saver.py
  - code_analysis/core/cst_tree/tree_sidecar.py
  - code_analysis/core/cst_tree/tree_metadata.py
  - code_analysis/core/cst_tree/node_stable_id.py
  - code_analysis/core/cst_tree/node_type_utils.py
  - code_analysis/core/cst_tree/skeleton.py
  - code_analysis/core/cst_module/patcher.py
  - code_analysis/core/cst_module/patcher_insert.py
  - code_analysis/core/cst_module/patcher_create.py
  - code_analysis/core/cst_module/blocks.py
  - code_analysis/core/cst_module/validation.py
  - code_analysis/core/cst_module/docstring_validator.py
  - code_analysis/core/cst_module/models.py
  - code_analysis/core/cst_module/utils.py
  - code_analysis/core/mutable_cst/
  - code_analysis/cst_query/
  do_not_take:
  - code_analysis/core/cst_tree/node_id_markers.py
  - code_analysis/commands/
  - code_analysis/core/database/
  - code_analysis/core/database_client/
  - code_analysis/core/database_driver_pkg/
  - code_analysis/core/file_handlers/
  - code_analysis/cli/
  forbidden_patterns:
    inline_comment_transport_forbidden: set_stable_id, get_stable_id and ANY use of
      @node-id comments as mutation transport are FORBIDDEN. They violate the single-assignment
      rule for stable_id. node_stable_id.py may only be used for strip_inline_node_id_lines_from_source
      at parse time (defensive cleaner for legacy files).
    libcst_immutable_tree_workaround: 'stable_id survival across mutations uses ONLY:
      previous_metadata_map (exact_key position matching) and previous_obj_to_id (object
      identity mapping). No inline comment embedding.'
    read_side_strip_helpers_allowed: strip_persisted_node_ids and strip_inline_node_id_lines_from_source
      are allowed only as defensive read-time cleaners at parse. They must not write
      files and must not be used in mutation flow.
    sidecar_write_forbidden_in_parse: write_sidecar_atomic must not be called from
      CSTFormatter.parse. Sidecar is written only from save orchestration.
    stable_id_single_assignment: stable_id is assigned exactly once per node at first
      _build_tree_index. Never changed, never re-embedded in source, never extracted
      from source during mutation. Persisted in sidecar. This is an immutable data
      binding.
    write_side_marker_functions_forbidden: append_persisted_node_ids, render_marker_block
      must not be called anywhere in adapted code.
    in_memory_registry_forbidden: _trees dict, get_tree, remove_tree, start_cst_tree_ttl_cleanup,
      load_file_to_tree belong to the server session layer, not CSTFormatter.
    file_reading_forbidden_in_formatter: 'CSTFormatter.parse receives raw_content:
      str from editor core. Editor core owns disk I/O.'
forbidden_code_paths:
- ai_editor/editor_core/
- ai_editor/search/
- ai_editor/interfaces/
- ai_editor/transaction_writer/
- ai_editor/formatters/text/
- ai_editor/formatters/ai/
- ai_editor/formatters/foreign.py
- .venv
- venv
- site-packages
allowed_dependencies:
  may_depend_on:
  - G-010 AbstractFormatter contract
  - libcst >= 1.1
  - lark (cst_query XPath parser)
  may_be_used_by:
  - G-009 editor core and buffer registry (opens .py via CSTFormatter)
  - G-011 universal search layer (iter_units via CSTFormatter)
  - G-008 public interfaces (validate_file for .py)
public_contracts_provided:
- CSTFormatter (implements AbstractFormatter)
- CSTTree (document model, opaque to editor core)
- stable_id (durable node address surviving mutations)
- skeleton view (declarative read mode)
- cst_query specific command (XPath-like search)
formatter_registration:
  extension: .py
  formatter_name: cst
document_model:
  type: CSTTree
  description: 'In-memory LibCST tree with node index. Opaque to editor core. address
    is stable_id: a UUID assigned once at first parse, never changed.'
  key_fields:
  - tree_id: runtime session UUID
  - module: libcst.Module object
  - metadata_map: node_id -> TreeNodeMetadata (carries stable_id)
  - node_map: node_id -> libcst.CSTNode
  - node_id_aliases: old_node_id -> current_node_id (transitive, updated after each
      mutation)
  - root_node_id: stable_id of module root
stable_id_contract:
  address_for_callers: stable_id only. node_id is internal. All public operations
    accept and return stable_id.
  assignment_rule: 'stable_id is assigned EXACTLY ONCE per node: at the moment the
    node first appears in _build_tree_index. Value = node_id at first assignment (UUID4).
    It is stored in TreeNodeMetadata.stable_id and in sidecar metadata_map. It is
    NEVER reassigned, never moved, never overwritten. If a node cannot be matched
    after mutation, it is treated as a new node and gets a new UUID4.'
  no_inline_markers: set_stable_id and get_stable_id from node_stable_id.py are NOT
    used in the adapted CSTFormatter. Embedding @node-id comments into source as a
    mutation transport mechanism violates the single-assignment rule. These functions
    exist in cst-code legacy but must not appear in the adapted code.
  persistence: stable_id persists in sidecar metadata_map under node_id key. Loaded
    on next parse when both checksums match. Never changes between sessions.
  resolution: find_by_stable_id(stable_id) -> TreeNodeMetadata -> node_id -> libcst
    object. Linear scan over metadata_map.values().
  rule: stable_id is assigned once. Never changes. Never re-embedded. Never re-extracted
    from source code.
  strip_usage: strip_inline_node_id_lines_from_source is used ONLY at parse time as
    a defensive cleaner for legacy files that may contain @node-id comments from old
    cst-code. Not used during mutation.
  survival_across_mutations: 'Before mutation: snapshot tree.metadata_map as previous_metadata_map.
    Run mutation via LibCST transformation. After mutation: call _build_tree_index(tree,
    previous_metadata_map=previous_metadata_map, previous_obj_to_id=prev_obj_to_id).
    For each rebuilt node: (1) try exact_key lookup (start_line, start_col, end_line,
    end_col, type) in previous_metadata_map -> copy stable_id; (2) if not found by
    position, try previous_obj_to_id (id(libcst_object) -> node_id) -> copy stable_id;
    (3) if not found by either: node is new -> stable_id = new UUID4. No inline comment
    transport. No set_stable_id. No get_stable_id.'
sidecar_contract:
  path_pattern: '{py_dir}/.cst/{py_stem}.tree'
  purpose: Persist node identity (stable_id, metadata_map) and checksums so the tree
    can be reloaded without re-parsing when the source has not changed.
  two_checksums:
    source_sha256: SHA-256 of UTF-8 bytes of logical Python source (tree.module.code
      at write time). Validated on load against SHA-256 of fresh .py content stripped
      of any legacy trailing marker block.
    tree_sha256: SHA-256 of the JSON body bytes of the sidecar file. Written into
      the header line. Validated on load to detect sidecar corruption or partial write.
  format: 'Line 1: CST_TREE_V1 sha256=<source_sha256_hex> tree_sha256=<tree_body_sha256_hex>

    Line 2+: JSON body'
  json_body_fields:
  - format_version
  - source_sha256
  - tree_sha256
  - root_node_id
  - path_to_node_id (path_indices string -> node_id)
  - metadata_map (node_id -> TreeNodeMetadata.to_dict with stable_id)
  - metadata_node_order
  - parent_map
  - node_id_aliases
  validation_on_load:
  - 1. Read sidecar. If missing or unreadable -> skip sidecar, build from scratch.
  - 2. Parse header. Extract source_sha256 and tree_sha256.
  - '3. Validate tree_sha256: SHA-256(json_body_bytes) must equal tree_sha256. Mismatch
    -> sidecar corrupt, build from scratch.'
  - 4. Compute SHA-256(logical_source) from .py file content (strip legacy trailing
    marker block first). Must equal source_sha256. Mismatch -> source changed, build
    from scratch.
  - 5. Load metadata_map from sidecar. Call sidecar_matches_built_tree to confirm
    path->node_id layout. Mismatch -> build from scratch.
  - '6. All checks pass: use sidecar metadata_map directly, skip libcst re-parse.
    module still parsed from logical_source so libcst object graph is live.'
  written_at: After editor_core writes .py to disk (buffer.save / buffer.save_as).
    CSTFormatter.write_sidecar(py_path, tree) called by save orchestration. NOT written
    during parse.
  atomic_write: Write to .tmp file in .cst/ dir then os.replace to final path.
parse_contract:
  error_contract: libcst.ParserSyntaxError -> CSTFormatter re-raises as FORMAT_VALIDATION_FAILED
    with CST_PARSE_FAILED diagnostic. Editor core catches and falls back to text formatter
    (G-009 formatter_fallback_policy). Empty file -> return empty CSTTree with empty
    module body, no error. No auto-fix of syntax errors inside CSTFormatter.
  lifecycle_open_without_sidecar:
  - '1. Receive raw_content: str from editor core.'
  - '2. Strip legacy trailing marker block: strip_persisted_node_ids(raw_content)
    -> logical_source. (Defensive: new files will have no markers.)'
  - '3. Strip legacy inline @node-id comment lines: strip_inline_node_id_lines_from_source(logical_source).
    (Defensive: new files will have none.)'
  - 4. Compute source_sha256 = SHA-256(logical_source.encode('utf-8')).
  - '5. If file_path given: attempt sidecar load per sidecar_contract.validation_on_load.
    Both checksums must pass. If valid -> load metadata_map from sidecar, go to step
    9.'
  - '6. Parse: module = libcst.parse_module(logical_source). If ParserSyntaxError
    -> raise FORMAT_VALIDATION_FAILED + CST_PARSE_FAILED. Do not attempt auto-fix.'
  - '7. Build index: _build_tree_index(tree, previous_metadata_map=None). Each node:
    stable_id = node_id (new UUID). stable_id stored in TreeNodeMetadata.'
  - 8. Return CSTTree. Sidecar NOT written here.
  - 9. Return CSTTree. module parsed from logical_source so libcst graph is live.
    metadata_map from sidecar if step 5 succeeded.
  lifecycle_open_with_valid_sidecar: source_sha256 matches AND tree_sha256 matches
    AND sidecar_matches_built_tree passes -> metadata_map loaded from sidecar, module
    parsed from source, _build_tree_index skipped. Fast path.
  method: 'parse(raw_content: str, file_path: str | None = None) -> CSTTree'
  note: CSTFormatter.parse receives content as string from editor core. Editor core
    owns file I/O. file_path is optional metadata used only to locate the sidecar.
render_contract:
  method: render(document) -> str
  output: tree.module.code
  guarantee: clean Python source, no markers of any kind
save_orchestration:
  owner: editor core (buffer.save / buffer.save_as)
  steps:
  - 1. validate_document(document) -> ValidationResult. Failure -> abort, return error.
  - 2. render(document) -> python_source string.
  - 3. Editor core writes .py file atomically.
  - '4. CSTFormatter.write_sidecar(py_path, tree): compute source_sha256 = SHA-256(tree.module.code);
    serialise metadata_map to JSON body; compute tree_sha256 = SHA-256(json_body_bytes);
    write ''CST_TREE_V1 sha256=<source_sha256> tree_sha256=<tree_sha256>'' as header
    line, then JSON body, atomically to .cst/<stem>.tree.'
  - 5. tree.disk_source_sha256_hex = source_sha256.
mutation_operations:
  inline_comment_transport:
    invariant: THIS MECHANISM IS NOT USED IN THE ADAPTED CSTFORMATTER. set_stable_id/get_stable_id
      inline comment transport violated the single-assignment rule. Legacy reference
      only — do not implement.
    legacy_note: cst-code tree_builder.py used @node-id leading_lines comments in
      FunctionDef/ClassDef as a mutation transport. This is superseded by previous_metadata_map
      + previous_obj_to_id approach.
    status: REMOVED
  libcst_constraints_requiring_workarounds:
    Match_node_rejection: Match node body is not directly insertable. Require MatchCase
      as parent_stable_id; reject Match with clear error.
    SimpleStatementSuite_expansion: SimpleStatementSuite cannot accept inserted children.
      Auto-expand one-liner to IndentedBlock before insert.
    docstring_positional: Docstring is positional Expr at body[0]. replace_docstring
      detects and handles this explicitly.
    immutable_tree_recreation: 'All nodes recreated after any transformation. Stable_id
      survival: previous_metadata_map exact_key matching + previous_obj_to_id (id(obj)->node_id).
      No inline comment transport.'
    transformer_object_identity: CSTTransformer matches by object identity. node_map
      holds pre-mutation objects; previous_obj_to_id maps post-mutation recovery.
  note: All operations accept and return stable_id as address. node_id is internal.
  operations:
  - op: insert
    params:
    - parent_stable_id
    - position
    - code
    position_values:
    - first
    - last
    - after:<stable_id>
    special_cases:
    - empty file: create Module with docstring + first node
    - SimpleStatementSuite parent: auto-expand to IndentedBlock before insert
    - Match node as parent: reject with hint to use MatchCase
    stable_id_survival: previous_metadata_map exact_key + previous_obj_to_id in _build_tree_index
      after mutation
  - note: descendants deleted automatically by LibCST
    op: delete
    params:
    - node_stable_id
    stable_id_survival: previous_metadata_map exact_key + previous_obj_to_id
  - note: atomic delete + insert preserving node code
    op: move
    params:
    - node_stable_id
    - parent_stable_id
    - position
    stable_id_survival: previous_metadata_map exact_key + previous_obj_to_id
  - note: full node replacement; header-only replace for ClassDef/FunctionDef preserves
      body
    op: replace
    params:
    - node_stable_id
    - code
    stable_id_survival: previous_metadata_map exact_key
  - note: specialized replace for file/class/method docstring; inserts if absent,
      replaces if present
    op: replace_docstring
    params:
    - node_stable_id
    - text
    stable_id_survival: previous_metadata_map exact_key
  stable_id_survival_mechanism: 'Before mutation: snapshot tree.metadata_map as previous_metadata_map
    and build previous_obj_to_id = {id(libcst_node): node_id for each node in node_map}.
    Run mutation. After mutation: call _build_tree_index(previous_metadata_map=previous_metadata_map,
    previous_obj_to_id=previous_obj_to_id). For each rebuilt node: try exact_key (start_line,
    start_col, end_line, end_col, type) -> get stable_id from prev_meta; if not found
    try previous_obj_to_id; if not found: new node, new UUID4. No inline comments.'
libcst_constraints_and_workarounds:
  inline_comment_transport_removed: set_stable_id / get_stable_id inline @node-id
    comment transport is NOT used. This mechanism violates the single-assignment rule
    and was a legacy approach in cst-code. In the adapted CSTFormatter, stable_id
    never moves through source code.
  libcst_immutable_tree: 'All nodes recreated after any transformation. Workaround:
    previous_metadata_map exact_key + previous_obj_to_id in _build_tree_index. This
    is the ONLY stable_id survival mechanism.'
  match_node_rejection: 'Match node body is not directly insertable. Workaround: require
    MatchCase as parent_stable_id; reject Match with clear error.'
  simple_statement_suite_expansion: 'SimpleStatementSuite cannot accept inserted children.
    Workaround: auto-expand one-liner to IndentedBlock before insert.'
  docstring_positional: 'Docstring is not a named child but positional Expr at body[0].
    Workaround: replace_docstring detects and handles this pattern explicitly.'
  transformer_object_identity: 'CSTTransformer matches by object identity (is), not
    by position. Workaround: node_map holds pre-mutation objects; previous_obj_to_id
    maps id(libcst_object)->node_id for post-mutation recovery.'
read_operations:
  default_buffer_format: declarative. When editor core opens a .py file via CSTFormatter
    the view returned to the caller is the declarative overview, not full source.
    Full source available on demand via get_unit(stable_id).
  declarative:
    alias_function: build_declarative_overview(tree) -> (text, outline_nodes)
    default: true
    description: Declarative view of file structure without implementation bodies.
      Every element carries its stable_id prefixed as [stable_id] in the signature
      line.
    hides:
    - method bodies
    - function bodies
    - complex initializers
    method: 'skeleton(document: CSTTree) -> str'
    placeholder: 'Function and method bodies show: # Implementation hidden; request
      node by node_id for full code'
    shows:
    - file docstring
    - import statements with stable_id
    - class definition header with stable_id
    - class docstring
    - class properties (Assign / AnnAssign) with stable_id and first line
    - method signatures with stable_id
    - method docstrings
    - function signatures with stable_id
    - function docstrings
    stable_id_in_output: Every visible element prefixed with [stable_id] so caller
      can address nodes directly for get_unit or mutation.
  get_unit:
    description: Returns full source code of the node identified by stable_id, including
      implementation body.
    method: 'get_unit(document: CSTTree, stable_id: str) -> unit'
  iter_units:
    description: Iterates over visible units for search layer (G-011). Each unit carries
      stable_id and declarative metadata.
    method: 'iter_units(document: CSTTree, scope: str | None) -> Iterator[unit]'
  list_units:
    description: Returns flat list of all nodes with stable_id, type, kind, name,
      qualname, start_line, end_line. No source bodies included. Used for navigation
      and as cst_query result metadata.
    method: 'list_units(document: CSTTree) -> List[unit]'
specific_commands:
  cst_query:
    description: XPath-like search over document nodes using Lark-based selector parser.
    implementation: cst_query package (existing Lark grammar, parser, executor)
    result_format: declarative. Each matched node is presented as declarative overview
      (signature + docstring + children signatures, no bodies). Same rules as skeleton().
      Full body available via get_unit(stable_id).
    returns: 'List of nodes. Each entry: stable_id, type, name, qualname, start_line,
      end_line. Implementation body not included by default.'
    selector_examples:
    - //FunctionDef[@name='foo']
    - class > method:first
    - Def:*[start_line>=100]
    - function[@name^='_']:not([name^='__'])
validation:
  validate_content:
    input: raw Python source string
    behavior: parse via libcst; return syntax errors as diagnostics
    used_by: validate_file for .py in G-008
  validate_document:
    input: CSTTree
    behavior: compile tree.module.code; return compile errors as diagnostics
verification:
  scope: global_completion_gate
  commands:
  - python -m pytest tests/formatters/cst
  expected:
  - 'parse round-trips without loss: render(parse(src)) == src for valid Python.'
  - stable_id survives insert, delete, replace, move operations via previous_metadata_map.
  - 'stable_id not in source: no @node-id comments and no # cst-node-ids blocks in
    .py after any operation.'
  - 'sidecar written after save with two checksums: source_sha256 and tree_sha256.'
  - 'sidecar fast path: if both checksums match on reload tree is not re-parsed via
    _build_tree_index.'
  - 'sidecar corrupted (tree_sha256 mismatch): tree rebuilt from scratch.'
  - 'source changed (source_sha256 mismatch): tree rebuilt from scratch.'
  - skeleton returns only declarative elements with stable_id; method bodies replaced
    by placeholder comment.
  - 'skeleton shows: file docstring, imports, class headers+docstrings+properties,
    method signatures+docstrings, function signatures+docstrings. All prefixed with
    [stable_id].'
  - get_unit(stable_id) returns full source code of single node including body.
  - list_units() returns flat list with stable_id and no bodies.
  - cst_query returns matching nodes in declarative format; full body via get_unit.
  - replace_docstring inserts docstring when absent, replaces when present.
  - SimpleStatementSuite parent auto-expanded to IndentedBlock before insert.
  - delete removes node and all descendants.
  - move is atomic (no partial state on failure).
  - CSTFormatter.parse raises FORMAT_VALIDATION_FAILED on libcst.ParserSyntaxError;
    editor core applies text fallback.
  - validate_document uses compile() not libcst.parse_module and does not write files.
acceptance_criteria:
- CSTFormatter implements AbstractFormatter contract in full.
- document model is CSTTree; public address is stable_id; node_id is internal only.
- stable_id is stored in TreeNodeMetadata.stable_id and persists in sidecar metadata_map.
- stable_id survives mutations via previous_metadata_map exact_key + previous_obj_to_id
  as primary mechanism.
- 'For structural mutations (insert/delete/move): set_stable_id/get_stable_id used
  transiently in-memory for FunctionDef/ClassDef position shifts; strip_inline_node_id_lines_from_source
  called after recovery; comments never reach render() or disk.'
- 'Sidecar has two checksums: source_sha256 (SHA-256 of logical Python source) and
  tree_sha256 (SHA-256 of sidecar JSON body). Both validated on load.'
- 'If either checksum fails or sidecar is missing: tree built from scratch via libcst.parse_module
  + _build_tree_index.'
- Sidecar is written only from save orchestration (buffer.save / buffer.save_as),
  not from parse.
- parse(raw_content) receives string content from editor core. Editor core owns disk
  I/O. file_path is optional and used only to locate sidecar.
- 'render(document) produces clean Python source with no markers of any kind. No @node-id
  comments. No # cst-node-ids blocks.'
- 'Default read format is declarative: skeleton() / build_declarative_overview().
  Full source available via get_unit(stable_id).'
- 'skeleton() shows: file docstring, imports, class headers+docstrings+properties+method
  signatures+method docstrings, function signatures+docstrings. Every element prefixed
  with [stable_id].'
- get_unit(stable_id) returns full source code of a single node including body.
- list_units() returns flat list of all nodes with stable_id, type, kind, name, qualname,
  start_line, end_line. No bodies.
- cst_query results presented in declarative format (signature + docstring, no bodies).
  Full body via get_unit(stable_id).
- CSTFormatter has no global state (_trees, TTL loop). Tree lifetime managed by editor
  core buffer registry.
- 'validate_content: libcst.parse_module raises -> FORMAT_VALIDATION_FAILED + CST_PARSE_FAILED
  diagnostic.'
- 'validate_document: compile(tree.module.code) -> diagnostics on SyntaxError.'
- append_persisted_node_ids and render_marker_block absent from adapted code.
- All five mutation operations (insert, delete, replace, move, replace_docstring)
  work correctly with stable_id addressing.
- SimpleStatementSuite parent auto-expanded to IndentedBlock before insert. Match
  node rejected with clear error.
model_assignment_spec:
  performer: coding_agent
  instruction: 'Implement CSTFormatter under ai_editor/formatters/cst/ by adapting
    cst-code as specified in source_code_origin. Do not rewrite from scratch. Adapt
    tree_builder: remove _trees registry, TTL cleanup loop, load_file_to_tree (reads
    disk). CSTFormatter.parse(raw_content: str, file_path=None) receives string content
    from editor core. stable_id survival: primary = previous_metadata_map exact_key
    + previous_obj_to_id in _build_tree_index. Supplementary for structural mutations
    (insert/delete/move) only: call set_stable_id() before mutation to embed @node-id
    in FunctionDef/ClassDef leading_lines; call get_stable_id() after mutation to
    recover; call strip_inline_node_id_lines_from_source() to remove all comments
    before final _build_tree_index; comments must never appear in render() output
    or on disk. Do NOT use inline comment transport for replace or replace_docstring.
    Sidecar header: ''CST_TREE_V1 sha256=<source_sha256> tree_sha256=<tree_sha256>''.
    Validate both checksums on load. Write sidecar only from save orchestration. Default
    read format is declarative (skeleton). get_unit(stable_id) returns full node source.
    list_units() flat metadata list without bodies. cst_query results in declarative
    format. Do not call append_persisted_node_ids or render_marker_block. Do not edit
    editor_core, search, interfaces, transaction_writer, text formatter, ai formatter,
    foreign formatter, .venv, venv, or site-packages.'
observations_template:
- command: ''
  expected: ''
  actual: ''
  error: ''
  root_cause: ''
  fix: ''
  post_fix_verification: ''
  status: ''
formatter_contract_extensions:
  parse_exception_contract:
    behavior: libcst.parse_module raises cst.ParserSyntaxError; CSTFormatter.parse
      catches it and re-raises as FORMAT_VALIDATION_FAILED with CST_PARSE_FAILED diagnostic
    caller_behavior: editor core catches FORMAT_VALIDATION_FAILED and falls back to
      text formatter (see G-009 formatter_fallback_policy)
    must_raise_on:
    - any Python source that libcst.parse_module cannot parse
    - structurally corrupt .py file
    must_not_raise_on:
    - syntactically valid Python regardless of style
    - 'empty file: return empty CSTTree with empty module body'
  validate_document_contract:
    behavior: compile tree.module.code via compile() built-in; return ValidationResult
      with syntax diagnostics on failure
    called_by: editor core as first stage of save/save_as pipeline and by validate(buffer_id)
    must_not:
    - write files
    - modify document
    - re-parse from disk
