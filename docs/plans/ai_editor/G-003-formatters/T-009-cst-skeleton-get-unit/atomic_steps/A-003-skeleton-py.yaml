step_id: A-003
parent_tactical_step: T-009
name: Create declarative skeleton builders
target_file: ai_editor/formatters/cst/skeleton.py
operation: create_file
priority: 1
depends_on:
- A-001
concepts:
- C-033
- C-031
status: done
prompt: |
  Project: ai_editor. Tactical step: T-009. File: ai_editor/formatters/cst/skeleton.py. Operation: create_file.

  MRS C-033 DeclarativeSkeleton: line format `[stable_id] start-end  representation`; method bodies hidden with placeholder; visible kinds extended with property, classmethod, staticmethod, attribute, variable.

  Imports:
  ```python
  from __future__ import annotations
  import ast
  from typing import Any
  from ai_editor.formatters.cst.models import CSTTree
  from ai_editor.formatters.cst.tree_node_metadata import TreeNodeMetadata
  ```

  Constants:
  ```python
  BODY_PLACEHOLDER = "# ... implementation hidden ..."
  VISIBLE_KINDS = frozenset({
      "module", "import", "class", "function", "method",
      "property", "classmethod", "staticmethod", "attribute", "variable",
  })
  ```

  def _line_label(meta: TreeNodeMetadata, code: str) -> str:
      """Return `[stable_id] start-end  representation` per C-033."""
      if meta.kind == "import":
          rep = code.strip().splitlines()[0] if code.strip() else "import"
      elif meta.kind in {"function", "method", "property", "classmethod", "staticmethod"}:
          rep = _header_only(code)
      elif meta.kind in {"class", "module"}:
          rep = _header_only(code) or meta.kind
      else:
          rep = code.strip().splitlines()[0] if code.strip() else meta.kind
      return f"[{meta.stable_id}] {meta.start_line}-{meta.end_line}  {rep}"

  def _header_only(code: str) -> str:
      """First def/class line through trailing colon; skip # @node-id lines."""
      parts: list[str] = []
      for line in code.splitlines():
          s = line.rstrip()
          if s.strip().startswith("# @node-id:"):
              continue
          if not s and not parts:
              continue
          parts.append(s.strip())
          if s.strip().endswith(":"):
              break
      return " ".join(parts)

  def _docstring_lines(code: str, indent: str) -> list[str]:
      try:
          doc = ast.get_docstring(ast.parse(code))
      except SyntaxError:
          return []
      if not doc:
          return []
      return [f'{indent}"""', *[f"{indent}{ln}" for ln in doc.splitlines()], f'{indent}"""']

  def build_declarative_overview(tree: CSTTree) -> tuple[str, list[dict[str, Any]]]:
      """Full module skeleton text and outline node dicts."""
      lines: list[str] = []
      outline: list[dict[str, Any]] = []
      root = tree.root_node_id
      if root:
          _append_overview(tree, root, 0, lines, outline)
      text = "\n".join(lines) + ("\n" if lines else "")
      return text, outline

  def build_node_declarative_overview(tree: CSTTree, stable_id: str) -> tuple[str, list[dict[str, Any]]]:
      """Skeleton for one node addressed by stable_id."""
      meta = tree.find_by_stable_id(stable_id)
      if meta is None:
          return "", []
      lines: list[str] = []
      outline: list[dict[str, Any]] = []
      _append_overview(tree, meta.node_id, 0, lines, outline)
      return "\n".join(lines) + ("\n" if lines else ""), outline

  def _append_overview(tree, node_id, depth, lines, outline) -> None:
      """Recursive visible-node renderer; function/method bodies replaced by placeholder."""
      # Port traversal rules from cst-code skeleton: skip IndentedBlock wrapper;
      # show module/class/import/assignments; recurse children in start_line order;
      # for function/method/property kinds append BODY_PLACEHOLDER at depth+1.
      # Each visible node appends _line_label + docstring lines; outline dict includes stable_id, kind, name, qualname, start_line, end_line, depth.

  Constraints: under 350 lines; no file_path on CSTTree — module line uses kind module only.
verification:
  type: import
  target: ai_editor.formatters.cst.skeleton
  expected: build_declarative_overview and build_node_declarative_overview import; VISIBLE_KINDS includes property and variable.
