step_id: T-009
parent_global_step: G-002
name: Save and save_as pipeline — validate, render, upload via CA client
status: needs_review
description: 'Save pipeline delegates to formatter WritePipeline: export, temp, linters, atomic rename.'
concepts:
- C-013
- C-014
inputs:
- name: G-001 contracts
  type: ai_editor.contracts
  description: OperationResult, ValidationResult, ErrorCode.
- name: G-002/T-001 output
  type: ai_editor.editor_core.ca_client
  description: CodeAnalysisClient with upload_file and list_project_files.
outputs:
- name: ai_editor/editor_core/save_pipeline.py
  type: file
  description: "run_validate(formatter, document) -> ValidationResult. run_save_pipeline(formatter, document, relative_path: str, ca_client,\n  project_id: str, commit_message: str) -> OperationResult.\nrun_save_as_pipeline(formatter, document, new_relative_path: str, ca_client,\n  project_id: str, overwrite: bool, commit_message: str) -> OperationResult.\nInternal _source_bytes(formatter, document) -> bytes. upload_file in run_save_pipeline always unlock_after_write=False. upload_file in run_save_as_pipeline always unlock_after_write=False.\n"
atomic_steps: []
cascade_note: changed — write orchestration via base WritePipeline
