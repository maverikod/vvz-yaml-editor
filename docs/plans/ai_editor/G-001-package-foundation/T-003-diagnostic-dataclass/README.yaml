step_id: T-003
parent_global_step: G-001
name: Diagnostic dataclass
status: draft
description: "Create Diagnostic frozen dataclass. Atomic unit carried inside every
  result type. Must be defined before result envelopes (T-004) which embed list[Diagnostic].
  No business logic. No ErrorCode import (uses plain str for code field).\n"
concepts:
- C-003
inputs:
- name: package_skeleton
  type: T-001 output
  description: ai_editor/contracts/ package must exist.
outputs:
- name: ai_editor/contracts/diagnostic.py
  type: file
  description: Diagnostic frozen dataclass with fields code(str), message(str), 
    path(str|None), details(dict).
atomic_steps:
- A-001
